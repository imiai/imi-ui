import React from "react";

interface ITooltipProps {
    title: string;
    placement: 'top | left | right'
}

const Tooltip = () => {
    return (
        <div>
            <span>{}</span>
        </div>
    )
}